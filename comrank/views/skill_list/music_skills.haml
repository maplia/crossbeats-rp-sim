%div.unit
  %h2= type_name
  %div.subunit
    - unless skills.present?
      %p 該当する曲のプレイ成績がありません
    - else
      %table.skill_list
        - unless mobile?
          %thead
            %tr
              %th.number #
              %th.title タイトル
              %th.level レベル
              %th.point RP
              %th.rate RATE
              - if CxbRank::SiteSettings.rev_mode?
                %th.u_rate S/U
              - else
                %th.u_rate ULT
              %th.rank ランク
              %th.combo コンボ
              %th コメント
        %tbody
          - row = 1
          - skills.each do |skill|
            - next if !edit and (skill.iglock_best_point || 0.0) == 0.0
            = haml :music_skill, :locals => {:skill => skill,
              :edit => edit, :ignore_locked => ignore_locked, :row => row, :bonus => bonus}
            - row = row + 1
