%table.skill_chart
  - unless mobile?
    %thead
      %tr
        - if CxbRank::SiteSettings.cxb_mode?
          %th.number #
        %th.title タイトル
        - CxbRank::SiteSettings.music_diffs.keys.sort.each do |diff|
          %th{:class => CxbRank::MUSIC_DIFF_CLASSES[diff], :colspan => 2}= CxbRank::SiteSettings.music_diffs[diff]
  %tbody
    - skills.each do |skill|
      = haml :skill, :locals => {:skill => skill}
